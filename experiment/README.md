# Experiment

Lightweight tests, demos, and sanity checks for implementations in `concept/` live here.

Examples:

- Small scripts that compare expected and actual outputs.
- Minimal benchmark experiments.
- Edge-case checks for a single algorithm.
